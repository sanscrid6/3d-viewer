export enum ProviderType {
  DataSource = 'DATA_SOURCE',
  ApiKey = 'API_KEY',
  User = 'USER',
  Subscription = 'SUBSCRIPTION',
  Redis = 'REDIS',
}

export enum HeaderType {
  UserDevice = 'x-user-device',
}

export enum CommonErrors {
  UserDontExist = 'User dont exist',
}
